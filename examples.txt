# VERIFIED
npx tsx src/cli/index.ts api test -t avs \
  -i "Verify valid address returns VERIFIED"
# ✅ 1 passed

# CORRECTED  
npx tsx src/cli/index.ts api test -t avs \
  -i "Test address with missing state returns CORRECTED"
# ✅ 1 passed

# NOT_VERIFIED
npx tsx src/cli/index.ts api test -t avs \
  -i "Test UN VERIFIED address returns NOT_VERIFIED"
# ✅ 1 passed

UI recording 

./fully-auto-record.sh 'Chewy Test' 'Complete flow' 'https://www-stg.chewy.net' --clean

npm run generate:avs "Test INVALID address which returns NOT_VERIFIED"